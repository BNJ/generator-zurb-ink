language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-zurb-ink' ]; then cd .. && eval "mv $currentfolder generator-zurb-ink" && cd generator-zurb-ink; fi

